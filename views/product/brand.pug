extends ../layouts/common

block content
    .brandContainer(style='background-image : url('+backgroundImage+')')
        .container
            h1.text-center.pt-5
                a(href="#info").text-decoration-none.text-dark= brand.name 
            .contentWrap.d-flex.flex-column
                .info#info.overflow-hidden
                    p= brand.description
                .product.container.mt-3
                .row
                    if products
                    each product in products
                        .col-lg-2.mt-3.col-6.col-md-4
                            a.card_link(href= '/product/' + product.id)
                                .pro_card
                                    .card_image
                                        img(src= product.image alt='')
                                        .special
                                            p(style="visibility:hidden") Mới về
                                            if product.discountedPrice
                                                p SALE
                                    .card_content
                                        p.card_name= product.name
                                        a.brand(href='')= product.brand
                                        .price
                                            if !product.discountedPrice
                                                p.final.numberPrice= product.price 
                                                    | &#x20AB;
                                                p(style="visibility:hidden").mb-2.before= product.discountedPrice
                                                    | &#x20AB;
                                            else 
                                                p.final.numberPrice=product.discountedPrice 
                                                    | &#x20AB;
                                                p.mb-2.before=product.price 
                                                    | &#x20AB;
                                        .add2cart.d-flex.justify-content-center
                                            a(href="/product/addToShoppingCart/" + product.id).btn.btn-primary.btn-sm  Thêm vào giỏ hàng                
    script( type="text/javascript" src="js/script.js")
